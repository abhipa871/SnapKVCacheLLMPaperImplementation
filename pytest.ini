[pytest]
markers =
    gpu: CUDA + HF Qwen3.5-0.8B integration tests
    padding: mask and logical position helper tests
    snapkv: SnapKVCache and attention mask path tests
    chunk_prefill: multi-forward prefill and gate tests
    integration: mini-model forward integration tests
